npm run dev -w platform
npm run dev -w tenant  


uvicorn app.main:app --reload


npm install @hotel/types -w tenant
npm ls @hotel/types -w tenant





const { showLoader, hideLoader } = useLoader();

bg-[var(--primary)]
text-[var(--text)]
text-[var(--secondary)]

TODO:
1. Byt ut allt till apiClient.api

Tömma i databasen:
TRUNCATE TABLE
    tenant_property_themes,
    tenant_knowledge,
    pages,
    tenants
RESTART IDENTITY CASCADE;


Ta bort tenant
docker exec -it hotel-postgres psql -U platform -d platform
SELECT id, name, subdomain
FROM tenants;

DELETE FROM tenants
WHERE id = 'TENANT_ID_HÄR';